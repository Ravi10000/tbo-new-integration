import { NextFunction, Request, Response } from "express";

export function injectTBOCredentials(req: Request, res: Response, next: NextFunction) {
    try {

    } catch (err) {
        console.log({ err })
    }
}